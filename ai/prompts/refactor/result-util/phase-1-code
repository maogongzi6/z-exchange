Please continue the result/error-handling refactor in this repo.

First read:
- ai/codex/refactor/result-util/result-error-handling-implementation-checklist.md
- ai/codex/refactor/result-util/result-error-handling-evaluation.md

Context
- I have already implemented the basic `ErrorCode` and `Result` interfaces in:
  - common-util/src/main/java/com/exchange/common/result
- Some concrete implementation is intentionally left incomplete
- `IdempErrorCode` is already implemented as an example subsystem error code
- The current work is basically phase 1/2/3 of `result-error-handling-implementation-checklist.md`

Your tasks
1. Inspect the current code in:
   - common-util/src/main/java/com/exchange/common/result
2. Follow the prior evaluation in:
   - ai/codex/refactor/result-util/result-error-handling-evaluation.md
3. Refactor the current record-style `Result` into a final class:
   - use `final class Result<T>`
   - use a private constructor
   - put `success(...)`, `failure(...)`, and `from(...)` as static factory methods on `Result`
4. Complete `ProtoErrorMapper`:
   - map from `ErrorCategory` to `ErrorCodePb`
   - `ErrorCodePb` is defined in:
     - grpc-proto/common-proto/src/main/proto/com/exchange/proto/common/error.proto
5. This project has not gone online yet:
   - no need for backward compatibility
6. Do not change any other files for now
7. Create a new branch under:
   - ai/codex/refactor

Constraints
- Keep the change minimal and focused
- Do not refactor unrelated code
- Do not touch ledger/wallet/common boundary code yet
- Only complete the common result/error foundation requested above
- Preserve current design direction unless the evaluation doc clearly requires an adjustment

Deliverables
- completed `Result<T>` final class with private constructor and static factory methods
- completed `ProtoErrorMapper`
- minimal focused diff
- brief summary of what changed and any remaining follow-up work, write into ai/codex/refactor/result-util
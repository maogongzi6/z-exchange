Review and evaluate the current Result / error-handling design in this repo, and assess a proposed refactor.

Focus paths
- common-util/src/main/java/com/exchange/common/utils/result
- ledger-service/src/main/java/com/exchange/app/ledger/result
- wallet-service/src/main/java/com/exchange/app/wallet/result

My proposed refactor

1. ErrorCode
- Replace `PbMappableErrorCode` with an `ErrorCode` interface:
  - `getErrorCode()`
  - `getMessage()`
- Provide a default `ErrorCode` implementation in `common-util`
- Other services/components should use the common default implementation by default
- Each service/component defines its own `ErrorCode` constants

2. Result
- Provide a `Result` interface with:
  - `isSuccess()`
  - `getErrorCode()`
  - `getDetail()`
  - `getScope()`
- Add `scope` to represent where the `Result` comes from
- Provide a default `Result` implementation
- Consolidate `Results` utility into a single implementation in `common-util`
- `Results` should no longer check concrete `ErrorCode` type

3. Proto error mapping
- `ErrorCode` no longer stores `ErrorCodePb`
- Each service converts business `ErrorCode` -> `ErrorCodePb` at the boundary
- Each service maintains its own mapping
- If an `ErrorCode` is not mapped, convert it to `ERROR_INTERNAL`

Future only, do not implement now
- In `common-util`, throw exceptions internally
- Catch exceptions at boundaries and convert them to `Result`, likely via AOP
- Add an advanced `Result` implementation to preserve result chain

Your work
1. Inspect current project code, mainly Result and error-handling related code
2. Evaluate the current Result strategy: strengths, smells, risks, coupling, extensibility problems
3. Evaluate my refactor design: whether it is a good refactor, what problems it solves, what new problems it may introduce
4. Give concrete suggestions and improvements

Output requirements
- Be precise and critical
- Prefer project-specific evaluation, not generic theory
- Highlight trade-offs clearly
- Call out any hidden risks around layering, proto boundary mapping, cross-service consistency, and future evolvability
- If my design has weak points, propose better alternatives
- Do not implement code; this is design evaluation only
Continue the Result refactor in the current branch.

First read:
- ai/codex/refactor/result-util/result-error-handling-implementation-checklist.md

Your scope
- Inspect code in `common-util`, focusing on current `Result` usage
- Refactor `common-util` to use the new Result design in:
  - `common-util/src/main/java/com/exchange/common/result`

Allowed during implementation
- Create new subsystem `ErrorCode` enum(s) if necessary
- Add new enum values to `IdempErrorCode` or newly created `ErrorCode` enum(s) when needed
- Add static utility methods to:
  - `common-util/src/main/java/com/exchange/common/result/Result.java`
  when necessary

Do not
- Change the refactored Result design structure
- Change irrelevant logic in `common-util`
- Change `ledger-service` or `wallet-service`

Notes
- Focus only on `common-util`
- It is acceptable if `wallet` / `ledger` is temporarily broken due to the `Result` refactor in `common-util`

Deliverable
- Generate a change doc in:
  - `ai/codex/refactor/result-util`
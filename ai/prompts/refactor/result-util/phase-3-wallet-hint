Start wallet-service Result refactor by copying the ledger-service pattern, not by inventing a new one.

First read:
- ai/codex/refactor/result-util/result-error-handling-ledger-account-migration.md
- ai/codex/refactor/result-util/result-error-handling-ledger-post-migration.md

Do this first:
- create `WalletServiceErrorCode`
- create `WalletBoundaryErrorMapper`
- create wallet-side `ProtoErrorMapper`
- tighten wallet `PbErrorBuilder` so the new path accepts only `WalletServiceErrorCode`

Migration order:
1. `CreateWalletProcessor` + `WalletServiceImpl` create-wallet path
2. `GetBalanceSnapshotProcessor` + balance query boundary in `WalletServiceImpl`
3. transaction entry processors:
   - `AtomicTransactionProcessor`
   - `ReserveTransactionProcessor`
   - `ApplyReservationTransactionProcessor`
4. transaction helper/step classes:
   - `IdempPrecheckProcessor`
   - `BeforePostLedgerProcessor`
   - `AfterPostLedgerProcessor`
   - `UpdateReservationProcessor`
   - `ValidateHelper`
   - `ObjectBuilder`
5. infra helpers:
   - `BalanceSnapshotStore`
   - `DefaultPublisher`
   - `OutboxHelper`
   - `DefaultListener`
6. remote clients:
   - `PostServiceClient`
   - `AccountServiceClient`

Rules copied from ledger:
- replace `com.exchange.common.utils.result.Result` with `com.exchange.common.result.Result`
- replace `Results.fail(result)` with `Result.failure(result)`
- replace `result.success()/value()/errorDetail()/errorCode()` with `isSuccess()/getValue()/getDetail()/getErrorCode()`
- remove `Results.getErrorCode(...)` from active callers
- business code may return wallet-owned error codes directly when public meaning is clear
- foreign/common errors must not leak to callers; map them through `WalletBoundaryErrorMapper`
- keep `PbErrorBuilder` mechanical; put policy in boundary mapper, not builder

Wallet-specific warning:
- `PostServiceClient` is the highest-value cleanup point; do not keep the current lossy `POST_TRANSACTION_FAILED + reply.getError().getMessage()` behavior
- wrap remote ledger failures centrally and preserve upstream code/message/detail inside local detail

Finish like ledger:
- migrate wallet exceptions to shared `com.exchange.common.result.error.ErrorCode`
- delete wallet-local `result/ErrorCode.java`
- delete wallet-local `result/Results.java`
- verify `wallet-service` main and test sources have no `com.exchange.common.utils.result` imports left

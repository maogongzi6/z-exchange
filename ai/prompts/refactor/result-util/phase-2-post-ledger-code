Continue the Result refactor in the current branch.

you have read:
- ai/codex/refactor/result-util/result-error-handling-implementation-checklist.md
- ai/codex/refactor/result-util/result-error-handling-common-util-migration.md
- code in `common-util`, focused on:
      legacy `Result` in common-util/src/main/java/com/exchange/common/utils/result
      current `Result` in refactored Result strategy in common-util/src/main/java/com/exchange/common/result

what you have done:
- refactor common-util Result logic
- refactor ledger-service AccountServiceImpl Result logic

First read:
- ai/codex/refactor/result-util/result-error-handling-ledger-account-migration.md (most important)

Your scope
- Inspect code in `ledger-service`, focus on legacy Result logic and ErrorCode cast on boundaries
- Refactor the left `ledger-service` to use the new Result design in:
    common-util/src/main/java/com/exchange/common/result
- refactor with a similar approach in result-error-handling-ledger-account-migration.md and AccountServiceImpl related flow

Allowed during implementation
- limit your change in ledger-service and `common-util/src/main/java/com/exchange/common/result (if necessary)`
- Use LedgerBoundaryErrorMapper, LedgerServiceErrorCode, PbErrorBuilder, ProtoErrorMapper
- modify PbErrorBuilder if necessary
- Add new enum values to LedgerServiceErrorCode when needed
- Add static utility methods to:
  - `common-util/src/main/java/com/exchange/common/result/Result.java` when necessary

Do not
- Change the refactored Result design structure
- Change irrelevant logic in `common-util`/`ledger-service`
- Change `wallet-service`

Notes
- Focus only on `ledger-service`
- It is acceptable if `wallet-service` / `ledger-service` is temporarily broken due to the `Result` refactor

Deliverable
- Generate a change doc in:
  - `ai/codex/refactor/result-util`


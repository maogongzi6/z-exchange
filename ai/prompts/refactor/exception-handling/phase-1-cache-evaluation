I have designed a cache exception design in common-util/src/main/java/com/exchange/common/redis/aop (the code has not been completed, only shows the design).

Focus:
common-util/src/main/java/com/exchange/common/redis
common-util/src/main/java/com/exchange/common/exception

my design:
1. catch low level exceptions by CacheExceptionTranslateAop in BaseCacheReadSupport implementations and transform them to customized cache exception.
 1.1 remove CacheParseException and VersionedCacheParseException, replacing by CacheException with proper CacheErrorCode.
 1.2 add CacheErrorCode value when necessary, for example (name properly)
    CONNECTION,
    TIMEOUT,
    ACCESS,
    SERIALIZATION,
    MALFORMED_VALUE,
    SCRIPT,
    CONFIGURATION,

2. do not return Result<?> in common-util/src/main/java/com/exchange/common/redis/cache/component/support, return types like String/void/Boolean directly.

3. downgrade CacheException to Result with proper ErrorCode by CacheExceptionToResultAop on the boundaries of strategy.
cast non-CacheException to Result with a ErrorCode=UncaughtInternalError
 compare these two design:
 3.A use a specific CacheExceptionToResultAop to cast CacheException to Result (like current design);
  also use OutboxExceptionToResultAop to cast OutboxException to Result in the future. so as other sub system
 3.B use a global ExceptionToResultAop to cast all exception in common-util to Result.

Your work
1. Inspect current project code
2. Evaluate the current Error handling strategy: strengths, smells, risks, coupling, extensibility problems
3. Evaluate my refactor design: whether it is a good refactor, what problems it solves, what new problems it may introduce
4. Give concrete suggestions and improvements

Output requirements
- Be precise and critical
- Prefer project-specific evaluation, not generic theory
- Highlight trade-offs clearly
- Call out any hidden risks around layering, cross-service consistency, and future evolvability
- If my design has weak points, propose better alternatives
- Do not implement code; this is design evaluation only
- output your evaluation to ai/codex/refactor/error-handling
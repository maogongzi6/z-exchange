based on what we discussed

I want you to refactor the code:
1.
  Project-specific recommendation:
  - ValueCodec.encode(...), VersionCodec.encode(...), RawCacheWriter.set(...),VersionCacheWriter.setIfAbsentOrNewer()
    VersionCacheWriter.setIfAbsentOrNewer(...) can throw CacheException with one of these cache-specific codes instead of UNEXPECTED_INTERNAL:
      - INVALID_ARGUMENT
      - INVALID_STATE
      - or one minimal code like CONTRACT_VIOLATION

  - Decode-time failures should stay as they are: MALFORMED_VALUE / SERIALIZATION.
  - Self-recover in get() should still only apply to MALFORMED_VALUE / SERIALIZATION, not these new programmer-misuse
    codes.
  - Wallet/ledger should use suppress-exception strategy beans; fail-fast callers can still use plain strategies.

2. Keep the suppressing strategy boundary catching CacheException only.
3. Use explicit suppressing strategy decorators or direct proxy interceptors in StrategyFactory.
4. Wire wallet/ledger to the suppressing variants.

Your work
1. Inspect current project code
2. do previous refactor
3. generate a change doc in ai/codex/refactor/error-handling, also evaluate the refactor
4. commit with a proper comment

Constraints
- Keep the change minimal and focused
- Do not refactor unrelated code
- Do not touch ledger/wallet unrelated business logic and other sub system in common-util
- Preserve current design direction unless the evaluation doc clearly requires an adjustment
- you can skip test
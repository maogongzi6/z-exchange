for what you mentioned in cache-code-implementation.md.:
    Notable Adjustment From The Evaluation
    The earlier evaluation preferred keeping clear programmer-misuse cases out of cache exceptions where possible.

    Current implementation keeps that only partially:

    writer null-value misuse now throws IllegalArgumentException
    some codec encode-contract failures are still represented as CacheException(UNEXPECTED_INTERNAL)
    That keeps the refactor minimal while still removing the old parse-exception model.

current requirement in ledger/wallet says, cache exception should not block main flow

I suggest a design:
1. implement a new AOP to downgrade IllegalArgumentException/IllegalStateException to UNEXPECTED_INTERNAL, with a careful and precise comment and error log
2. in StrategyFactory, build suppress-exception strategies with AspectJProxyFactory, add CacheExceptionToResultAop and the new created AOP
3. refactor UNEXPECTED_INTERNAL to IllegalArgumentException/IllegalStateException in ValueCodec/VersionCodec when needed
4. switch current strategy in wallet/ledger-service to CacheExceptionToResult-based bean

Your work
0. the design is not complete in code, just evaluate it
1. Inspect current project code
2. Evaluate the my design
3. Give concrete suggestions and improvements

Output requirements
- Be precise and critical
- Prefer project-specific evaluation, not generic theory
- If my design has weak points, propose better alternatives
- Do not implement code; this is design evaluation only
- output your evaluation to ai/codex/refactor/error-handling

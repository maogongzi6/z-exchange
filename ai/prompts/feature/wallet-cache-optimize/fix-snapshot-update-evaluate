Target:
If balance freshness matters, fix entity cache coherency first

If `GetSnapshotByRefId` is expected to reflect current balances, then this optimization is secondary.

What is missing today:

- cache refresh or invalidation after snapshot updates in `BeforePostLedgerProcessor`
- cache refresh or invalidation after snapshot updates in `AfterPostLedgerProcessor`

Design:
I suggest to refactor balance update flow in
BeforePostLedgerProcessor.updateSnapshotsBeforePosting()
    balanceSnapshotManager.reserveFromWalletId()
    balanceSnapshotManager.releaseFromWalletId()

AfterPostLedgerProcessor.updateSnapshotsAfterPosting()
    balanceSnapshotManager.transferOutFromWalletId()
    balanceSnapshotManager.transferInToWalletId()

refactored flow could refer to
    WalletReservationRepository.selectInIdForUpdate()
    WalletReservationRepository.updateWithInOptimisticLock()

refactored flow:
inside db txn:
1. select * by PK id in for update, to get and lock the balance snapshot first
2. update locked  balance snapshot in memory
3. update to db in the format of: mapper.update(now, wrapper); like WalletReservationRepository.updateWithInOptimisticLock()

after db commit:
1. use BalanceSnapshotStore.afterDbUpdate() to execute strategy for each updated snapshot

your work:
1. inspect related code
2. Evaluate whether the design is reasonable, whether it is a good change, what problems it solves, what new problems it may introduce
3. Be strict and realistic. Give concrete suggestions and improvements
4. output an evaluation and possible improve suggestion.

Output requirements
- Be precise and critical
- Prefer project-specific evaluation, not generic theory
- Highlight trade-offs clearly
- If my design has weak points, propose better alternatives
- Do not implement code; this is design evaluation only
- output your evaluation (.md) to ai/codex/feature/
refer to ai/codex/feature/wallet-snapshot-optimize/hot-balance-snapshot.md

Refactor Plan:
1. sort balance snapshot by PK in memory and lock rows in PK order by selectIdInForUpdate.
(sorting in memory is mainly for batch operation. for each batch, mysql will lock in PK order)
2. aggregate all actions for a wallet and apply them to the locked snapshot in memory, then perform db update for each snapshot.
3. validate snapshot before updating, any invalidation must rollback the db txn:
    RESERVE must check available >= amount.
    RELEASE must check reserved >= amount.
    TRANSFER_OUT must check reserved >= amount.
    All updates must check wallet status and asset match.
    All updates must preserve WalletStatus.OPEN.
4. only update changed fields
    create a new empty snapshot, set changed fields, and update with
        mapper.update(toUpdate, wrapper);
    mybatis-plus uses NOT_NULL update strategy by default which means only non-null fields will be updated.
    do not manually update `updatedAt`, it is handled by mybatis-plus interceptor
5. for version: set old version in `toUpdate` variable, and add PK comparator in wrapper like:
    wrapper.eq(BalanceSnapshot::getId, id)
6. update by the current style: update with an `toUpdate` entity object and a wrapper to verify necessary fields on db level
7. after db txn, call balanceSnapshotStore to do postDbUpdate operation, only take effect to id->snapshot cache, do not update ref cache.
8. cache freshness is a best-effort operation, not a guarantee. API get data through cache should tolerate temporarily mismatch/stale
9. do not change existing PromotionClassifier, it will be optimized in the future.

Your work
0. refer to ai/codex/feature/wallet-snapshot-optimize/hot-balance-snapshot.md but stick strictly to this doc
1. Inspect current project code (if you have not done this before)
2. do previous refactor with proper comments
3. generate a change doc in ai/codex/feature/wallet-snapshot-optimize, also evaluate whether my refactor follows read ai/codex/feature/wallet-snapshot-optimize/hot-balance-snapshot.md
4. commit with a proper comment

Constraints
- Keep the change minimal and focused
- Do not refactor unrelated code
- Do not touch unrelated business logic
- Preserve current design direction unless the evaluation doc clearly requires an adjustment
- you can skip test

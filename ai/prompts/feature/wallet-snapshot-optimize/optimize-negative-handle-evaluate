I'm optimizing the negative handle flow in ledger-service/src/main/java/com/exchange/app/ledger/dao/store/LedgerTxnStore.java

Task:
    Fix negative-cache renewal

    The store needs to distinguish:

    - cache negative hit
    - DB miss

    and only set negative on real DB miss.

    Without that, repeated miss traffic can pin a negative result far longer than intended.

My Design:
Design A:
    return Result<QueryInfo<LedgerTxn>> in doGetByRefId(),
    QueryInfo is a record of: LedgerTxn + ResultType
    ResultType is a enum of: DbHit, CacheHit, TombStone, NegativeCache, Miss

    int getByRefId() only set negative if it is a Miss

Design B:
    return Result<T> from RawCacheStrategy.get()/VersionCacheStrategy.get()
    add Tombstone for tombstone
    add NegativeCache for negative
    add CacheMiss for cache miss

    in getByRefId() only set negative if it is a Miss

your work:
1. inspect related code
2. Evaluate whether the design is reasonable, whether it is a good change, what problems it solves, what new problems it may introduce
3. Be strict and realistic. Give concrete suggestions and improvements
4. output an evaluation and possible improve suggestion.

Output requirements
- Be precise and critical
- Prefer project-specific evaluation, not generic theory
- Highlight trade-offs clearly
- If my design has weak points, propose better alternatives
- Do not implement code; this is design evaluation only
- output your evaluation to ai/codex/wallet-snapshot-optimize/feature/
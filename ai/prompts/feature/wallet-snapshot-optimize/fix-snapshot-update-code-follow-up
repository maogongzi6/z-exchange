I have a suggestion, pls evaluate them:
1. gather snapshot amount validations like in AfterPostLedgerProcessor.updateSnapshotAfterPosting()/BeforePostLedgerProcessor.updateSnapshotBeforePosting():
    if (snapshot.getAvailable() < action.getAmount()) {
        return Result.failure(WalletServiceErrorCode.BALANCE_SNAPSHOT_UPDATE_FAILED,
            String.format("failed to reserve amount, available not enough, snapshot: %s, action: %s", snapshot, action));
    }

gather these validation in UpdateBalanceSnapshotProcessor.java, to validate all amount fields of changed snapshot must be larger or equals to zero
this could make the validation rule cleaner and safer in future changes

your job:
 evaluate my suggestion, do not change code for now
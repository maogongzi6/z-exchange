Investigate and fix a bug in ApplyReservationTransactionProcessor

Current bug:
ApplyReservationTransactionProcessor always calls beforePostingLedger(requestInfo, true) and has no operation-type validator.
ObjectBuilder.createOutbox(...) only emits ledger entries for TRANSFER_OUT / TRANSFER_IN,
so release-only apply requests still commit local reservation/snapshot changes and create an outbox payload that can contain zero ledger entries.

Task:
0. check out a branch from ai/codex/dev-refactor-cache and fix bug on it
1. Verify whether this reasoning matches the actual code.
2. Fix it with the smallest reasonable change.
3. Fix the bug by detecting pure release transaction in ApplyReservationTransactionProcessor before calling beforePostingLedger().
4. Keep transfer-related logic unchanged unless required by the fix.

Please avoid broad refactors. First understand the current flow, then patch the bug, then explain why the fix is correct.
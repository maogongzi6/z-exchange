Investigate and fix a bug in reservation state update after ledger posting.

Current suspected bug chain:
1. `AfterPostLedgerProcessor.getTxnInfo(...)` loads `CONSUME`, `TRANSFER_OUT`, and `TRANSFER_IN`.
2. `TransactionInfo.create(...)` only stores transfer actions in `walletIdToPostActions`.
3. `updateReservationAfterPosting(...)` iterates `walletIdToPostActions` and contains logic for `ActionType.CONSUME`.
4. Since `CONSUME` actions are missing from that collection, reservation state `pendingSettle -> consumed` never happens.

Task:
- check out a branch from ai/codex/dev-refactor-cache and fix bug on it
- Verify whether this reasoning matches the actual code.
- Fix it with the smallest reasonable change.
- Ensure reservation transition for `CONSUME` happens after successful posting.
- Keep transfer-related logic unchanged unless required by the fix.
- Add regression tests.

Please avoid broad refactors. First understand the current flow, then patch the bug, then explain why the fix is correct.
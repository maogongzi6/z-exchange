Please help me implement wallet balance snapshot query APIs in this repo.

Goal
Implement the two gRPC query APIs defined in:
grpc-proto/wallet-proto/src/main/proto/wallet/wallet_service.proto

APIs:
- rpc getSnapshotByWalletId(GetSnapshotByIdRequestPb) returns (GetSnapshotReplyPb);
- rpc getSnapshotByRefId(GetSnapshotByRefIdRequestPb) returns (GetSnapshotReplyPb);

Important working rules
0. First read:
   - ai/codex/hint.md
   Then create a new branch under:
   - ai/codex/feature
1. Add boundary code in:
   - wallet-service/src/main/java/com/exchange/app/wallet/service/WalletServiceImpl.java
2. Add a new query snapshot processor under:
   - wallet-service/src/main/java/com/exchange/app/wallet/processor/balance
3. Complete the query flow by following the ledger query example here:
   - ledger-service/src/main/java/com/exchange/app/ledger/processor/post/GetLedgerTxnProcessor.java
4. Implement a data store for BalanceSnapshot query operations, similar in role/style to:
   - ledger-service/src/main/java/com/exchange/app/ledger/dao/store/LedgerTxnStore.java
5. Treat query by ref id like ledgerTxnStore.getByRefId and use a stable cache strategy for that path
6. Do NOT implement cache logic for query by wallet id; leave that part alone
7. Generate a feature doc under:
   - ai/codex/feature
   The doc should explain the design, files changed, API flow, cache strategy, and any high-value optimization ideas for ledger/snapshot query flow
8. After modification, update:
   - ai/codex/hint.md
   if necessary

Implementation requirements
- Before coding, inspect the relevant wallet and ledger code paths and reuse existing project conventions, naming, layering, and error-handling style
- Follow existing processor pattern, store pattern, DTO/proto conversion style, and dependency injection style already used in the project
- Keep the change minimal and symmetric with the existing ledger query implementation where reasonable
- Do not introduce unrelated refactors
- Prefer consistency with existing query processor / store / service boundary design over inventing a new pattern
- Make sure proto-generated request/response types are wired correctly in the boundary layer
- Make sure not-found behavior matches existing project conventions for similar query APIs
- For ref-id query, follow the stable cache approach already used by ledger query-by-ref-id style if applicable in this repo
- For wallet-id query, implement the query path but leave cache logic untouched / unadded
- Reuse existing mapper / DAO / cache abstractions if they already exist; only add what is necessary

Suggested work sequence
1. Read ai/codex/hint.md
2. Inspect:
   - wallet_service.proto
   - WalletServiceImpl
   - existing wallet processors under wallet-service/.../processor
   - GetLedgerTxnProcessor
   - LedgerTxnStore
   - any existing snapshot entity / mapper / dao / cache code
3. Create the new branch under ai/codex/feature
4. Implement store support for BalanceSnapshot queries
5. Implement the new balance snapshot query processor
6. Wire the two gRPC APIs in WalletServiceImpl
7. you can skip tests in the project, and add necessary unit test
8. Write the feature doc in ai/codex/feature
9. Update ai/codex/hint.md if needed

Deliverables
- Working implementation for both APIs
- New BalanceSnapshot query processor
- New/updated BalanceSnapshot store logic
- Boundary wiring in WalletServiceImpl
- Feature doc in ai/codex/feature
- Any necessary hint.md updates

At the end, provide:
1. a concise summary of what changed
2. the key design choices
3. cache behavior for each API
4. any risks / follow-up suggestions
5. the list of files modified
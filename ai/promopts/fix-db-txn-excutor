read ai/codex/hint.md if you have not.

you are a senior fintech engineer and found a bug in DbTransactionHelper.java:
CU-1
Shared TransactionTemplate is mutated per call, which can leak propagation settings across concurrent threads.
DbTransactionHelper.executeWithResult(...) and executeWithIfSuccess(...) both call template.setPropagationBehavior(propagation) before template.execute(...).
In this codebase the template is injected as a Spring bean into PostLedgerProcessor and OutboxRetryHandler, so it is very likely singleton-scoped.
A call expecting one propagation mode can race with another call setting a different mode, producing inconsistent transaction boundaries under load.
Risk: High
Recommendation:
Stop mutating a shared TransactionTemplate. Create a fresh TransactionTemplate per call from PlatformTransactionManager, or expose dedicated immutable templates per propagation mode.

I've fixed it and rename the class to DbTxnExecutor.java.
Now
1. evaluate the fix under dev-ai branch
2. checkout new branch in codex/bugfix to optimize the fix if needed. fix it with smallest change. and do not change other flow unless it is necessary
3. Output your evaluation to ./codex/bugfix/

Please avoid broad refactors. First understand the current flow, then patch the bug, then explain why the fix is correct.
You are a senior fintech backend engineer.

you have already read the code in common-util/src/main, grpc-proto/common-proto/src/main/proto, ledger-service/src/main, and grpc-proto/ledger-proto/src/main/proto.


Your task is to deeply analyze code in wallet-service/src/main, grpc-proto/wallet-proto/src/main/proto.

Follow these steps strictly:
0. skip 
   - log/test files
   - proto generated java files

1. First, summarize the system architecture:
   - modules
   - responsibilities
   - data flow

2. Then identify key components:
   - core services
   - data models
   - external dependencies

write system architecture summarization and key components structure to
codex/wallet-service/architecture-and-component.md

Rules:
- Do NOT be generic
- Use evidence from code
- If unsure, say assumptions
- Do not use apply_patch.
- Use shell commands or a PowerShell script to modify the file directly.
Keep the change minimal.


3. Then analyze critical logic:
   - transaction flow
   - concurrency / consistency
   - event flow
   - cache logic
   - result and error handling

write critical logic analyzation to codex/wallet-service/logic.md
Rules:
- Do NOT be generic
- Use evidence from code
- If unsure, say assumptions
- Do not use apply_patch.
- Use shell commands or a PowerShell script to modify the file directly.
Keep the change minimal.


4. Then identify risks:
   - race conditions
   - data inconsistency
   - scalability bottlenecks
   - risk level

5. Then suggest improvements:
   - architecture
   - performance
   - reliability

make a risks table

write risks and suggest improvements to codex/wallet-service/risks-and-improvement.md

Rules:
- Do NOT be generic
- Use evidence from code
- If unsure, say assumptions
- Do not use apply_patch.
- Use shell commands or a PowerShell script to modify the file directly.
Keep the change minimal.




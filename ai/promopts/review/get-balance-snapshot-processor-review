Task: Review the recent hot-data cache strategy changes on branch `feature/cache-strategy-for-hot-data`.

Target review scope:
Review the latest two commits on this branch:
1. `support hot snapshot cache strategy`
2. `implementing hot path cache logic (NOT DONE)`

Rules:
0. First read:
   - `ai/codex/hint.md`
1. Before reviewing, inspect relevant code carefully. Do not review only the commit message or file names.
2. Find bugs and potential risks in both code and business flow, if any.
3. Provide concrete fix suggestions and optimization suggestions.
4. Generate a review document focused on `GetBalanceSnapshotProcessor`, and write it into:
   - `ai/codex/review`
5. Update `ai/codex/hint.md` if necessary.

Required workflow:

1. Read:
   - `ai/codex/hint.md`

2. Inspect git history and identify the exact latest two commits on branch `feature/cache-strategy-for-hot-data`.

3. Inspect relevant code before commenting. At minimum, review:
   - `GetBalanceSnapshotProcessor`
   - related cache strategy classes
   - hot snapshot routing / promotion / classifier logic
   - snapshot store / cache store / strategy wiring
   - relevant processor flow, especially read path and write/update interaction
   - related registration / Spring wiring / bean injection if touched
   - any tests added/changed for this feature
   - any related DAO/store/query code that affects snapshot query flow

4. Review from both perspectives:
   - Code correctness
   - Business flow correctness

Review focus:
- Whether hot snapshot vs normal snapshot routing is correct
- Whether cache strategy choice matches actual snapshot write/read behavior
- Whether hot path logic is complete or partially inconsistent
- Whether promotion/routing/classifier logic can misroute keys
- Whether cache-aside vs post-refresh usage is safe for current snapshot update style
- Whether stale cache / tombstone / negative cache risks exist
- Whether local promoted-key set / classifier behavior can cause incorrect routing
- Whether concurrency / race / invalidation issues exist
- Whether code structure introduces future maintainability risks
- Whether naming / abstraction / dependency design can be improved
- Whether the business/logic flow is complete

Expected output:
Generate a markdown review doc under:
- `ai/codex/review`

Suggested filename:
- `get-balance-snapshot-processor-review.md`

The document should include:
1. Scope reviewed
2. Relevant files / modules inspected
3. Current logic flow of `GetBalanceSnapshotProcessor`
4. How hot snapshot cache strategy currently works
5. Bugs found
6. Potential risks / edge cases
7. Business-flow risks
8. Fix suggestions
9. Optimization suggestions
10. Overall assessment
11. Open questions / incomplete parts, if any

Review quality requirements:
- Be specific and grounded in actual code
- Point to exact classes / methods / flows when possible
- Distinguish definite bugs from possible risks
- Distinguish code issues from business-flow issues
- Do not rewrite unrelated code
- Do not make speculative architectural changes unless they clearly improve this feature
- If no bug is found in some area, say so clearly
- If the implementation is incomplete, explain exactly what is missing and what it may break

Hint update rule:
- Update `ai/codex/hint.md` only if you discover a reusable review rule, coding rule, or cache-strategy design rule that should be preserved for future work
- Do not update it for one-off observations

Important constraints:
- This is a review task, not a large refactor task
- Prefer identifying issues and proposing fixes over making broad unrelated code changes
- If you do make any minor safe review-related edits, keep them minimal and clearly document them
- Base conclusions on the actual code, not assumptions
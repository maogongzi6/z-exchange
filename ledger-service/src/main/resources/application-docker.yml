spring:
  datasource:
    url: "jdbc:mysql://${DB_HOST:172.31.18.211}:${DB_PORT:3306}/${DB_NAME:trade_ledgerservice}?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true"
    username: ${DB_USERNAME:ledger}
    password: ${DB_PASSWORD:ledger-local}

  redis:
    host: ${REDIS_HOST:172.31.18.211}
    port: ${REDIS_PORT:6379}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:172.31.18.211:9092}

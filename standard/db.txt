1. always update in primary key order to prevent deadlock
2. DO NOT directly call mybatis update interface to do update, wrapper update logic in self-defined methods to ensure locking business key first and updating in business key order
3. also for `FOR UPDATE` statement, same as #2
4. maybe we should force lock statement using primary key
5. decide a table order when access table in transaction:
    wallet service: reservation -> snapshot -> txn -> action -> outbox




reserve: +reserve_amount, -wallet.available +wallet.reserve
consume: -reserve_amount +pending_settle -> -pending_settle +consumed
transfer_out: -wallet.reserve
transfer_in: +wallet.available



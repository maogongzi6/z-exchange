1. SET key:{service}:{scope}:{idemp_id}  P|reqHash|token NX EX 120s~300s.
2. if exists, parse.
3.a if reqHash mismatch → CONFLICT error.
3.b if state=P → return REQ_PROCESSING error, which means another same req is processing but still not persist db. Hint upstream to retry later.
3.c if state=A → return txnId.
4. DB lookup by idemp_key:
5. if found → set Redis state=A and return txnId.
6. if not found, business logic. Then, DB txn: insert txn (unique idemp_key) + insert outbox.
7. SET key:{service}:{scope}:{idemp_id} A|reqHash|txnId EX 24h
8. on Error, delete it to release the key. only delete it when token matches by running a CAS lua script
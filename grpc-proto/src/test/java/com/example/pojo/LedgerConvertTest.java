package com.example.pojo;

import org.junit.Test;

import java.io.IOException;

public class LedgerConvertTest {
    @Test
    public void testPostTransaction() throws IOException {
//        LedgerEntry entry = new LedgerEntry("account-id", LedgerDirection.DEBIT, 1000, "asset-id");
//        LedgerEntry entry2 = new LedgerEntry("account-id", LedgerDirection.DEBIT, 2000, "asset-id");
//        PostTransactionRequest request = new PostTransactionRequest("txn-id", "ref-id", "description", List.of(entry,entry2));
//
//        com.example.proto.ledger.PostTransactionRequestPb.Builder builder = com.example.proto.ledger.PostTransactionRequestPb.newBuilder();
//        ProtobufBeanHelper.toProtoBean(builder, request);
//
//        PostTransactionRequest request2 = ProtobufBeanHelper.toPojoBean(PostTransactionRequest.class, builder.build());
//
//        Assert.assertEquals(request, request2);
    }
}
